boolean isUniqueChars(String str) {

	if (str.length() > 128) {
		return false;
	}

	boolean[] charArr = new boolean[128];
	while (int i = 0; i < str.length; i++) {
		int val = str.charAt(i);
		if (charArr[val]) {
			return false;
		}
		else charArr[cal] = true;
	}
	return true; 
}
